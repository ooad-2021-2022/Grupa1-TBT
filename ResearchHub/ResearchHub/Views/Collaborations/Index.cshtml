@model IEnumerable<ResearchHub.Models.Collaborations>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.timeRequestMade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.collaborator)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.collaboratee)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.timeRequestMade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.collaborator.id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.collaboratee.id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
