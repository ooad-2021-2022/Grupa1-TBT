@using System.Reflection
@using System.ComponentModel.DataAnnotations
@model ResearchHub.Models.ResearchPaper


@{
    ViewData["Title"] = "Create";
}
<style>
    body{
        background-color: #d2f6f6;
    }
</style>
<div class="row" style="justify-content: center; background-color: #edfbfb;" >
    <div>
        <h1>Create</h1>
        <h4>ResearchPaper - potrebno još dodati kviz i korisnike koji su bili kolaboratori na paperu.</h4>
        <hr />
    </div>
    <div class="col-md-8">
        <form asp-action="Create" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="title" class="control-label"></label>
                <input asp-for="title" class="form-control" />
                <span asp-validation-for="title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="paperAbstract" class="control-label"></label>
                <textarea asp-for="paperAbstract" class="form-control">
                </textarea>
                <span asp-validation-for="paperAbstract" class="text-danger"></span>
            </div>
            <div>
                <h4>Select appropriate types of your research:</h4>
                    <table>
                        @foreach (var type in Enum.GetValues(typeof(ResearchType)))
                        {
                            <tr>
                                <td><input type="checkbox" name="types" value="@((int)type)"/></td>                                
                                <td><label>@(ResearchHub.Controllers.ResearchPaperController.GetTypeName((ResearchType)type))</label></td>
                            </tr>
                        }
                    </table>
            </div>
            <div class="form-group">
                <h4 class="control-label">Enter authors of this paper, separated by comma:</h4>
                <input name="usernames" type="text" class="form-control"/>                
            </div>
            <div>
                <h4>Select appropriate topics of your research:</h4>
                    <table>
                        @foreach (var topic in Enum.GetValues(typeof(ResearchTopic)))
                        {
                            <tr>
                                <td><input type="checkbox" name="topics" value="@((int)topic)"/></td>                               
                                <td><label>@(ResearchHub.Controllers.ResearchPaperController.GetTopicName((ResearchTopic)topic))</label></td>
                            </tr>
                        }
                    </table>
            </div>
            
            <div class="form-group">
                <label asp-for="pdfFile" class="control-label"></label>
                <input asp-for="pdfFile" accept="application/pdf" />
                <span asp-validation-for="pdfFile" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Next step" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}